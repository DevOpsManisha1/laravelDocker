pipeline {
 agent 'any'
 options {
   skipStagesAfterUnstable()
 }
 stages {

// Build Development
   stage('DeplopyDevelopment') {
     when {
        branch 'main'
      }
     steps {
      //deploy steps go here
       sh """
          rsync -auvz -e ssh --exclude .git/ /var/lib/jenkins/workspace/DockerProject ubuntu@3.6.88.77:/home/ && chmod -R 775 storage
         """
     }
   }
 }
}
